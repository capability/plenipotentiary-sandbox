# =============================================================================
# FRONTEND ENV EXAMPLE
# =============================================================================

# Hostname for dev HMR (used in vite.config.ts hmr.host)
# Matches your Caddyfile subdomain for the frontend
PROJECT_SLUG=app-skeleton
FRONTEND_HOST=ui.${PROJECT_SLUG}.test

# Source map behavior in production builds
# Options: true | false | hidden | inline
# Default (if unset): false
SOURCEMAPS=false
